export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: '*',
      allow: '/',
      disallow: '/private/',
    },
    // Correction ici
    sitemap: 'https://nova-ent-website.vercel.app/sitemap.xml',
  };
}